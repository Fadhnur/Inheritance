#include <iostream>
#include "Object.hpp"

using namespace std;

Object::Object(int currentHealth, int maxHealth, int damage){
    currentHealth = currentHealth;
    maxHealth = maxHealth;
    damage = damage;
}
